// import { Outlet } from "react-router-dom";

export const SettingsRoot = () => {
  return <div style={{ padding: 20 }}>
    This will be the settings page. It will take things like API keys to be
    stored securely in electron-settings.
    {/* <Outlet /> */}
  </div>;
};
