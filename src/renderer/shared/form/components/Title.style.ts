import styled from "@emotion/styled";
import { COLORS } from "../../theme";
import { ParentheticalContainer } from "../../brackets";

export const StyledFormTitleContainer = styled(ParentheticalContainer)`
  color: ${COLORS.goldGlow};
  padding: 20px;
  text-align: center;
`;
